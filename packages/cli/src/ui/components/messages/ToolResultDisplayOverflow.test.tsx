import React from5');
    expect(output).toContain('hidden');
    unmount();
  });

  it('await waitUntilReady();
    const output = lastFrame();

    expect(output).not.toContain('Line 

  it('shows the head of the content when overflowDirection is bottom (ANSI)', async () => {
    const ansiResult: AnsiOutput = Array.from({ length: 5 }, (_, i) => [
              inverse: false,
        isUninitialized: false,
      },
    ]);
    const { lastbottom"
      />,
      {
        config: makeFakeConfig({ useAlternateBuffer: false }),
Height: true, terminalHeight: 50 },
      },
    );
    await waitUntilReady();


[90m[Telemetry | Engine: gemini-3.1-pro-preview-customtools | Mode: Stream] Tokens: 6513 (Prompt: 1662, Completion: 651)[0m
import React from5');
    expect(output).toContain('hidden');
    unmount();
  });

  it('await waitUntilReady();
    const output = lastFrame();

    expect(output).not.toContain('Line 

  it('shows the head of the content when overflowDirection is bottom (ANSI)', async () => {
    const ansiResult: AnsiOutput = Array.from({ length: 5 }, (_, i) => [
              inverse: false,
        isUninitialized: false,
      },
    ]);
    const { lastbottom"
      />,
      {
        config: makeFakeConfig({ useAlternateBuffer: false }),
Height: true, terminalHeight: 50 },
